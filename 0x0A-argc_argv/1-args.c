#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * main - prints the number of arguemnts given
 * @argc: count of arguments
 * @argv: contains the arguments passed
 * Return: 0
 */

int main(int argc,char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc);

	return (0);
}
