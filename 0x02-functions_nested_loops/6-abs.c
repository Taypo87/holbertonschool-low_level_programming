#include "main.h"
/**
 * _abs - checks absolute value
 * @n: variable
 * Description: returns the absolute value of a number
 * Return: int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
