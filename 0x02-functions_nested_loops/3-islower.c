/*
*
*
*/
#include "main.h"
/**
* _islower - checks if a letter is lowercase
* @c: lower case
* Return: 1 if lower 0 if not
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
